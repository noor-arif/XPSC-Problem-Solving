#include <bits/stdc++.h>
typedef long long int ll;
using namespace std;
void Solve()
{

    int x, y;
    cin >> x >> y;
    cout << x % 3 << '\n';
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        Solve();
    }
    return 0;
}